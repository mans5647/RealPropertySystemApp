<Page x:Class="RealPropertySystemApp.pages.AuthPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#FF4C566A"
      Title="Авторизация">

    <Page.Resources>
        <ResourceDictionary Source="..\Styles.xaml" />
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        

        <TextBlock Text="Логин" FontSize="16" FontWeight="Bold" Foreground="White" Grid.Row="0" Margin="5" />
        <TextBox Grid.Row="1" Height="30" FontSize="14" Margin="5" Name="login_input" />

        <TextBlock Text="Пароль" FontSize="16" FontWeight="Bold" Foreground="White" Grid.Row="2" Margin="5" />
        <PasswordBox Grid.Row="3" Height="30" FontSize="14" Margin="5" Name="password_input" />

        <Button  x:Name="doLoginButton"   Content="Войти" Width="150" Margin="10" Style="{StaticResource SwitchButtonStyle}" HorizontalAlignment="Center" />
    </Grid>
</Page>
